name: workflow-1
on: push

jobs:
  job-1:
    runs-on: ubuntu-24.04
    steps:
      - name: checkout
        uses: actions/checkout@v7

      - name: install dependencies
        run: npm install

      - name: test
        run: npm test

      - name: starting the app
        run: echo "your app is running on prot 3000"
